<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fondo blanco -->
    <solid android:color="@android:color/white"/>

    <!-- Borde verde -->
    <stroke
        android:width="2dp"
        android:color="#5BE2A7"/>

    <!-- Bordes redondeados -->
    <corners android:radius="16dp"/>
</shape>
